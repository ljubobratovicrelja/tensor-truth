name: gh_file
description: "Get contents of a file from a GitHub repository"
usage: "/gh_file <owner> <repo> <path>"
aliases: [ghf]

steps:
  - tool: get_file_contents
    params:
      owner: "{{args.0}}"
      repo: "{{args.1}}"
      path: "{{args.2}}"

requires_mcp: github

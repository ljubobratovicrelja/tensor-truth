name: arxiv
description: "Search arXiv for academic papers by keyword, author, or topic"
usage: "/arxiv <query>"
aliases: [ax]

steps:
  - tool: search_papers
    params:
      query: "{{args}}"
      max_results: 5
      sort_by: relevance

requires_mcp: simple-arxiv

name: gh_prs
description: "List pull requests for a GitHub repository"
usage: "/gh_prs <owner> <repo>"

steps:
  - tool: list_pull_requests
    params:
      owner: "{{args.0}}"
      repo: "{{args.1}}"

requires_mcp: github
